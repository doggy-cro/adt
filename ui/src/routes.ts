const server_url = 'http://localhost:5000';

export const routes = {
  chainData: `${server_url}/chain-data`,
  getFormData: `${server_url}/chain-data/form-data`,
  getPriceFromCPaprika: `${server_url}/price`,
};
